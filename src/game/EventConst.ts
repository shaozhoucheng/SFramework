module core {
     export const enum EventConst {
        /*-*begin sideMediator*-*/
        SIDE_MODULE_SHOW,

        SIDE_MODULE_HIDE,
        /*-*end sideMediator*-*/
        NEW_LOADER_START,
     }
}